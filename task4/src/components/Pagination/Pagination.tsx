import React from 'react';
import classes from '../../styles/pagination.module.scss';

function Pagination() {
  return (
    <div className={classes.pagination}>
      <h1 className={classes.title}>Pagination</h1>
    </div>
  );
}

export default Pagination;
